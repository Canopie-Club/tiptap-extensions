import SlashMenuComponent from "./SlashMenu.vue";

// Export the raw component
export const SlashMenu = SlashMenuComponent;

// Also re-export as default if needed
export default { SlashMenu: SlashMenuComponent };
